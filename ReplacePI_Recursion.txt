// Online C++ compiler to run C++ program online
#include <iostream>
using namespace std;

void RepPi(string s){
    if(s.length()==0){
        return;
    }
    if(s[0]=='p' && s[1]=='i'){
        cout<<"3.14";
        RepPi(s.substr(2));
    }
    else{
        cout<<s[0];
        RepPi(s.substr(1));
    }
}

int main() {
    // Write C++ code here
    RepPi("pippppipppi");
    

    return 0;
}